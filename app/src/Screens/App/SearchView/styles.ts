import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: { paddingTop: 10, paddingLeft: 20, paddingRight: 20, flex: 1 },
  searchQuery: { fontSize: 15, fontStyle: 'italic' },
})

export default styles
