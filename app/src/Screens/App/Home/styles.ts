import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  content: {
    paddingBottom: 20,
  },
  infoText: {
    textAlign: 'center',
    marginTop: 20,
  },
})

export default styles
