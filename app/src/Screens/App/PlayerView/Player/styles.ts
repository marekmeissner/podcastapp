import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    player: {
        flex: 1,
        display: 'flex',
        alignItems: 'center'
        },
    imageOverlay: {
        height: '80%',
        width: '95%',
        marginTop: 10
    },
    image: {
        height: '100%',
        width: '100%'
    }
})

export default styles