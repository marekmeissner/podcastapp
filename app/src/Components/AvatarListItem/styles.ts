import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  listItem: { borderBottomWidth: 0, paddingBottom: 20 },
  thumbnail: { height: 35, width: 35 },
  buttonText: { fontSize: 15 },
})

export default styles
